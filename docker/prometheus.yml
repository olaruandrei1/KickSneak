global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "dotnet-backend"
    static_configs:
      - targets: ["backend:5005"]
    metrics_path: /metrics